export type UserRegistration = {
  email: string;
  firstName: string;
  lastName: string;
  password: string;
};

export type UserSession = {
  email: string;
  firstName: string;
  lastName: string;
  fullName: string;
};
