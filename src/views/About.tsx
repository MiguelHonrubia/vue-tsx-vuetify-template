import { component } from "vue-tsx-support";

export const About = component({
  name: "About",
  render() {
    return (
      <div class="about">
        <h1>This is an about page</h1>
      </div>
    );
  },
});
